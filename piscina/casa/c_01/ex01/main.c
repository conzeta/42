#include <stdio.h>

int	main(void)
{
	int	av;
	int	bv;

	av = 10;
	bv = 5;
	printf("valores a y b: %d, %d\n", av, dv);
	ft_ultimate_div_mod(&av, &bv);
	printf("ahora a y b: %d, %d\n", av, dv);
	return (0);
}
